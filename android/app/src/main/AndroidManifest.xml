<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    
    <!-- Package visibility for Sunmi Printer Service -->
    <queries>
        <package android:name="woyou.aidlservice.jiuiv5" />
    </queries>

    <!-- Network permissions for Supabase -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Sunmi internal printer permissions -->
    <uses-permission android:name="com.sunmi.permission.INNERPRINTER" />
    <uses-permission android:name="woyou.permission.security.PRINT" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SpoonfulPOS">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
